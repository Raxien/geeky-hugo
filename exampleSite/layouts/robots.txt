# This robots.txt file controls crawling of URLs under {{ site.BaseURL }}.
# All crawlers are disallowed to crawl files in the "includes" directory, such
# as .css, .js, but Google needs them for rendering, so Googlebot is allowed
# to crawl them.
# /categories/ era bloccata quando le pagine erano ~119, frammentate e thin;
# dopo il consolidamento in poche macro-categorie con contenuto reale, ha
# senso lasciarle indicizzabili così possono posizionarsi per query tipo
# "viaggio in camper marocco".
User-agent: *
Disallow: /includes/
Disallow: /about/
Disallow: /press/

User-agent: Googlebot
Allow: /includes/
Disallow: /about/
Disallow: /press/

Sitemap: {{ site.BaseURL }}sitemap.xml
